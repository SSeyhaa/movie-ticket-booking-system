<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<!-- Reference to the custom parent -->
		<groupId>com.legend</groupId>
		<artifactId>starter-parent</artifactId>
		<version>1.0.0</version>  <!-- project version -->
		<relativePath>../starter-parent/pom.xml</relativePath>
	</parent>
	<artifactId>discovery-service</artifactId>
	<version>1.0.0</version>  <!-- project version -->
	<name>discovery-service</name>
	<description>
		The Movie Ticket Booking System is designed to allow users to browse available movies, select show times,
		book tickets, and make payments online. The system is composed of multiple microservices, each handling
		different parts of the application, like movie listings, theaters, payments, notifications, and user management.
	</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>

	<properties>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-server</artifactId>
			<version>${spring-cloud.version}</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
